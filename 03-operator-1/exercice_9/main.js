let paul, marie, eric, clair, madameMukuna, joseph, sarah;

// Ecrivez votre code ici
// Première categories
let totalPatrimoine = 60000000 + 40000000 + 20000000;
let premiereCategorie = (75 / 100) * totalPatrimoine;
let deuxiemeCategorie = (25 / 100) * totalPatrimoine;
paul = (1 / 3) * premiereCategorie;
marie = (1 / 3) * premiereCategorie;
eric = ((1 / 3) * premiereCategorie) / 2;
clair = ((1 / 3) * premiereCategorie) / 2;
madameMukuna = (1 / 3) * deuxiemeCategorie;
joseph = (1 / 3) * deuxiemeCategorie;
sarah = (1 / 3) * deuxiemeCategorie;
console.log(paul);

// Ne pas modifier le code ci-dessous
module.exports = {
  paul,
  marie,
  eric,
  clair,
  madameMukuna,
  joseph,
  sarah,
};
